@import '~src/vars'

.root
  display: inline-block
  font-size: $tag-text-size
  font-weight: 600
  padding: 3px 6px
  border-radius: 3px
  box-shadow: 0 1px 0 0 rgba(#000, 0.3)

.clickable
  cursor: pointer
